<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.RbacMapper">

    <!-- 用户权限聚合查询：users → user_roles → roles → role_permissions → permissions -->
    <select id="findUserPermissions" resultType="com.example.demo.vo.UserPermissionVO">
        SELECT p.id          AS id,
               p.code        AS code,
               p.name        AS name,
               p.description AS description,
               r.code        AS roleCode,
               r.name        AS roleName,
               ur.created_at AS grantedAt
        FROM user_roles ur
                 JOIN roles r ON ur.role_id = r.id
                 JOIN role_permissions rp ON rp.role_id = r.id
                 JOIN permissions p ON rp.permission_id = p.id
        WHERE ur.user_id = #{userId}
        ORDER BY p.id, r.id
    </select>

    <select id="countPermissionsByRoleId" resultType="int">
        SELECT COUNT(*)
        FROM role_permissions
        WHERE role_id = #{roleId}
    </select>
</mapper>
