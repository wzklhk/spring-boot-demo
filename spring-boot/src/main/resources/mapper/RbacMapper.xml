<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.RbacMapper">

    <!-- 用户权限聚合查询：user → user_role → role → role_permission → permission -->
    <select id="findUserPermissions" resultType="com.example.demo.vo.UserPermissionVO">
        SELECT p.id          AS id,
               p.code        AS code,
               p.name        AS name,
               p.description AS description,
               r.code        AS roleCode,
               r.name        AS roleName,
               ur.created_at AS grantedAt
        FROM user_role ur
                 JOIN role r ON ur.role_id = r.id
                 JOIN role_permission rp ON rp.role_id = r.id
                 JOIN permission p ON rp.permission_id = p.id
        WHERE ur.user_id = #{userId}
        ORDER BY p.id, r.id
    </select>

    <select id="countPermissionsByRoleId" resultType="int">
        SELECT COUNT(*)
        FROM role_permission
        WHERE role_id = #{roleId}
    </select>
</mapper>
